name: Screenshots · iOS

# Captures App Store product-page screenshots on an iOS Simulator and uploads
# them as an artifact to drag into App Store Connect. Simulator work is
# macOS-only, so this can't run on the Windows dev machine.
#
# Runs against the LIVE zoonze.com backend, so the shots show real catalog
# content (real products, real AED prices) — not placeholder data.
on:
  workflow_dispatch:
    inputs:
      locale:
        description: "Store locale to capture (en = LTR, ar = RTL)"
        type: choice
        default: en
        options: [en, ar]

jobs:
  capture:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.4
          channel: stable
          cache: true

      - name: Pre-create SPM SourcePackages dir (flutter/flutter rsync workaround)
        run: mkdir -p build/ios/SourcePackages
      - run: flutter pub get
      - name: Generate GraphQL typed Dart (build_runner)
        run: dart run build_runner build --delete-conflicting-outputs

      # The shipped app deliberately does NOT set FlutterDeepLinkingEnabled —
      # adding it would change release routing behaviour. The screenshot script
      # drives the app by deep link, so enable it for THIS simulator build only.
      # This edit lives in the runner's checkout and is never committed.
      - name: Enable Flutter deep linking (simulator build only)
        run: |
          /usr/libexec/PlistBuddy -c \
            "Add :FlutterDeepLinkingEnabled bool true" ios/Runner/Info.plist
          /usr/libexec/PlistBuddy -c "Print :FlutterDeepLinkingEnabled" ios/Runner/Info.plist

      - name: Capture screenshots
        env:
          SHOT_LOCALE: ${{ inputs.locale }}
        run: bash tool/ios_screenshots.sh

      - uses: actions/upload-artifact@v4
        with:
          name: appstore-screenshots-ios-${{ inputs.locale }}
          path: build/screenshots/ios/*.png
          if-no-files-found: error
