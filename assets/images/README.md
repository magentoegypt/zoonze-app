# In-app images

Commit these here:

- `banner.png` — home/diagnostics banner
- `test_product.png` — sample product image for dev/preview

(Referenced via `AppImages.banner` / `AppImages.testProduct` once the app build lands.)
