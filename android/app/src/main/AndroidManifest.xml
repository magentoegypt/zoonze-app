<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="Zoonze"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep links: custom scheme (zoonze://app/...) + https app links.
                 App-links require an assetlinks.json on the domain — see
                 docs/decisions/release.md. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="zoonze" android:host="app"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="zoonze.com"/>
            </intent-filter>
        </activity>

        <!-- FCM: route system-handled (background/killed) notifications to the
             app's high-importance channel, give them a valid white small icon
             (not the launcher icon → white square), and a burgundy accent. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="zoonze_default" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />

        <!-- N-Genius card entry. payment-sdk 5.2.3 declares every other activity
             with an SdkTheme.* theme but ships PaymentsActivity with none, so it
             inherits our application theme — which descends from
             android:Theme.Light, not AppCompat — and dies on launch with
             "You need to use a Theme.AppCompat theme (or descendant) with this
             activity". Give it one. Remove once the SDK declares its own. -->
        <activity
            android:name="payment.sdk.android.payments.PaymentsActivity"
            android:theme="@style/NGeniusPaymentTheme"
            tools:node="merge" />

        <!-- Samsung Pay: SpayValidity reads this key off our own <application>
             metaData (Bundle.getFloat("spay_sdk_api_level")) and refuses to bind
             if it is absent or above the bundled SDK (2.22.00). Declaring a
             wrong value yields ERROR_PARTNER_SDK_API_LEVEL (-10), which
             SamsungPaySession degrades to "unavailable" -> the row is hidden and
             checkout is otherwise unaffected. Keep it <= 2.22. -->
        <meta-data
            android:name="spay_sdk_api_level"
            android:value="2.18" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: open web links, email and phone from the app
             (footer links, Help & FAQ contact). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- Samsung Pay binds by explicit package to
             com.samsung.android.spay/.sdk.v2.service.CommonAppService. Under
             Android 11+ package visibility that bind silently fails without this
             entry, so getSamsungPayStatus would answer "not found" even on a
             Galaxy with Samsung Wallet installed. -->
        <package android:name="com.samsung.android.spay" />
    </queries>
</manifest>
